import 'package:flutter/material.dart';

abstract class AppColors {
  static const white = Color(0xffFFFFFF);
  static const lightGrey = Color(0xffCCCCCC);
  static const golderYellow = Color(0xff00BCD4);
  static const transportColor = Colors.transparent;
  static const primayColor = Color(0xff0D7377); // Buttons and headings
  static const backgroundColor = Color(0xff1E1E1E); // Buttons and headings
  static const backgroundColor2 = Color(0xff353F45); // Buttons and headings
  static const secondColor = Color(0xff14FFEC); // Background for sections/card
  static const accentColor =
      Color(0xffFF6F61); // Call-to-action buttons (e.g., "Contact Me")
  static const deepGrayColor =
      Color(0xff222831); // Text and background for readability
  static const lightGrayColor =
      Color(0xffEEEEEE); // Text and background for readability
}
